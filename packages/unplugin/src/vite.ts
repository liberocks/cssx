import { vite } from './index';

/**
 * Creates the CSSX adapter for Vite.
 *
 * @param options CSSX adapter options.
 * @returns The Vite plugin.
 */
const cssxVite = vite;

export default cssxVite;
