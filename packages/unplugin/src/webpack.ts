import { webpack } from './index';

/**
 * Creates the CSSX adapter for webpack.
 *
 * @param options CSSX adapter options.
 * @returns The webpack plugin.
 */
const cssxWebpack = webpack;

export default cssxWebpack;
