/** Maximum accepted length of a whitespace-separated utility list. */
export const MAX_UTILITY_LIST_LENGTH = 16_384;

/** Maximum nesting depth accepted while scanning user-controlled syntax. */
export const MAX_NESTING_DEPTH = 32;

/** Maximum number of named styles allowed in one source map. */
export const MAX_STYLE_MAP_ENTRIES = 10_000;

/** Maximum number of utility candidates allowed across one compilation. */
export const MAX_CANDIDATE_COUNT = 50_000;
