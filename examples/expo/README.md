# Expo example

This fixture uses Expo SDK 57 and the same `@cssxio/react-native` API as bare
React Native. Run `pnpm export:android` and `pnpm export:ios` to validate both
production bundles without requiring a simulator.
