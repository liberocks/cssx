# Bare React Native example

This fixture uses React Native 0.87 and the `@cssxio/react-native/babel` Metro
transform. Run `pnpm bundle:android` and `pnpm bundle:ios` for reproducible
production bundle checks without an emulator.
