{
  "name": "cssx-visual-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node ./scripts/run-matrix.mjs",
    "test:update": "node ./scripts/run-matrix.mjs --update",
    "test:coverage": "playwright test coverage --config playwright.config.ts",
    "test:motion": "playwright test motion --config playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
